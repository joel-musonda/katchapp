name: keep alive

on:
  schedule:
    - cron: '0 9 */6 * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: ping app
        run: curl https://genjutsu.xyz